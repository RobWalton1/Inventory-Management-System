<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Link to tailwind and daisyui style sheet-->
    <link rel="stylesheet" href="/styles/style.css">
    <title>Search result</title>
</head>
<body class="bg-hero min-h-screen bg-cover bg-no-repeat">
    <%- include('templates/navigation', { req: req }); %>
    <div class="hero min-h-screen">
        <div class="hero-content flex-col lg:flex-row  backdrop-blur-2xl ">
            <ul>
                <% for (let i = 0; i < results.length; i++) { %>
                    <li><strong>ID:</strong> <%= results[i].ID %></li>
                    </h1><li><strong>Product Name:</strong> <%= results[i].productName %></li>
                    <li><strong>Description:</strong> <%= results[i].Description %></li>
                    <li><strong>Quantity:</strong> <%= results[i].Quantity %></li>
                    <li><strong>Location:</strong> <%= results[i].Location %></li>
                    <li><strong>Price:</strong> £<%= results[i].Price %></li>
                    <hr>
                <% } %>
            </ul>
    </div>
    </div>

</body>
</html>